#include <bits/stdc++.h>
using namespace std;

int main()
{
	int n;
	vector <int> a(n);
	for(int &x : a)
		cin>>x;
	int q;
	cin>>q;
	vector <pair<pair<int, int>, int>>queries(q);
	for(auto &x : queries)
		cin>>
	return 0;
}